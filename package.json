{
  "name": "@bredele/email-inbox",
  "version": "1.0.0",
  "description": "Return email inbox URL based on a given email",
  "main": "dist/cjs/index.js",
  "files": ["dist"],
  "exports": {
    "import": {
      "types": "./dist/index.d.ts",
      "default": "./dist/esm/src/index.js"
    },
    "require": {
      "types": "./dist/index.d.ts",
      "default": "./dist/cjs/src/index.js"
    }
  },
  "types": "dist/index.d.ts",
  "scripts": {
    "build:cjs": "swc src -d dist/cjs --config-file .swc/cjs.json",
    "build:esm": "swc src -d dist/esm --config-file .swc/esm.json",
    "build": "npm run build:cjs && npm run build:esm && tsc --emitDeclarationOnly --outDir dist",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && node --test dist/cjs/src/index.test.js"
  },
  "keywords": ["email", "inbox", "url"],
  "author": "Olivier Wietrich",
  "license": "MIT",
  "dependencies": {
    "@bredele/email-provider": "^1.0.1"
  },
  "devDependencies": {
    "@swc/cli": "^0.7.7",
    "@swc/core": "^1.12.3",
    "@types/node": "^24.0.3",
    "typescript": "^5.8.3"
  },
  "publishConfig": {
    "access": "public"
  }
}
